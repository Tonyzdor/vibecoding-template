# API & DB Changes
- Minor changes without breaking can be applied immediately.
- Breaking changes should be flagged with a version or feature toggle and describe migration steps.
- For DB: the order is migration → code; rollback is the reverse migration.
